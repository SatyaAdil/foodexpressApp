pluginManagement {
    repositories {
        gradlePluginPortal() // ✅ WAJIB ADA
        google()
        mavenCentral()
    }
    plugins {
        id "dev.flutter.flutter-gradle-plugin" version "0.0.3"
    }
}

rootProject.name = 'your_project_name' // ganti sesuai
include ':app'
